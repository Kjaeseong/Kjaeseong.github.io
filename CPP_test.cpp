#include <iostream>
#include <vector>
using namespace std;


int main()
{
    int InputNum[10];
    vector<int> v;
    int Count = 0;

    for(int i = 0; i < 10; i++)
    {
        cin >> InputNum[i];
        InputNum[i] %= 42;

        if(v.size() != 0)
        {
            for(int j = 0; j < v.size(); j++)
            {
                if(v[j] )
            }
        }
        else
        {
            v.push_back(InputNum[i]);

        }

    }

    for(int i = 0; i < 10; i++)
    {
        for(int j = )
    }

    cout << Count;


}