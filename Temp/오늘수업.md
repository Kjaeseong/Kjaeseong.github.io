
# 오후수업 - 유니티 교재 2권 816쪽부터

## 좀비 서바이버 멀티플레이어
- 유니티에서는 포톤 솔루션 사용 가능.
- 현업에서는 쓰이는가..포톤이..글쎄
  - 포톤은 무료
- 국내에도 쓸만한 솔루션은 있다

### 네트워크 동기화

#### 로컬과 리모트
- 기존 교육했으니 패스
#### 동기화
- 이하동문
  
#### 로컬 권한 검사
- 플레이어 A의 조작은 A의 케릭터에만 적용되어야 한다.
- 업데이트에서 사용자 

### 게임 서버의 종류
- 서버 : 클라이언트들이 참가할 수 있는 네트워크 공간을 마련하는 컴퓨터(= 호스트)

#### 전용 서버
- 서버의 모든 자원이 온전히 네트워크 서비스를 유지하는 데 사용
- 서버가 플레이어로서 게임에 직접 참가하지 않는다.
- 고정비용 많이 발생


#### 리슨 서버
- 전용 서버가 없는 대신 플레이어 클라이언트 중 하나가 서버 역할을 맡는다.
- Play as Host 라고 부르기도 한다.
- 서비스 유지비용이 적다.
- 호스트의 컴퓨터 성능에 따라 네트워크 품질이 크게 달라진다.
- 호스트의 종료시 진행중인 게임을 정지하고 남은 클라이언트 중 새로운 호스트를 선정하는 절차 필요

#### P2P
- 게임에 참가한 클라이언트들 모두가 호스트 역할을 겸함
- 호스트에 연결된 구조가 아니라 서로 직접 연결된 형태
- 연산 대부분을 독점하는 특정 호스트가 없다
- 각자 자신의 월드에서 담당 연산 실행. 다른 클라이언트에 결과 전파
- 호스트가 게임 도중 접속 종료시, 호스트 교체 과정 불필요
- 서버 유지비용이 없다
- 프로그램의 처리 흐름을 직관적으로 짤 수 있다
- 참가자가 증가할수록 반응 속도가 느려진다.
  - 각 클라이언트가 부담하게 되는 연산량이 늘어난다.
  - 통상 16명을 참가자 상한선으로 여긴다.
  - 16명도 많은 편.
- 클라이언트는 해킹에 취약하다
  - P2P 방식은 수치 변조에 제일 취약하다.

#### 매치메이킹 서버
- 매치메이킹 과정에서 포톤에서 제공하는 전용 클라우드 서버 사용
- 매치메이킹 서버 : 
  - P2P나 리슨 서버라도 클라이언트들이 서로를 찾아 방 하나에 모이는 과정에서 사용할 전용 서버
- 룸이 구성되고 라운드를 시작할 시 플레이어중 한 명을 호스트로 삼는 리슨서버 방식 많이 사용

#### 포톤 룸
- 룸 : 여러 클라이언트가 모인 네트워크상의 가상의 공간
  - 유니티의 씬과 다른 계층에서 동작(씬이 아니다)
    - 유니티와는 따로 동작한다
  - 같은 룸에 있지만 서로 다른 씬을 로드하는것도 가능

### 네트워크 권한 분리
- 공정한 게임 결과를 보장하고 수치에 대한 위변조를 방지해야 한다.

#### 호스트에 위임
- 이유
  - 동기화에 오차가 존재하는 경우 기준이 되는 월드를 정하기 위해
  - 클라이언트의 변조나 위조 행위를 막기 위해
- 네트워크 동기화는 전송 속도나 패킷 손실 등의 현실적인 문제 때문에 완벽하지 않다.
- 게임 승패와 직접적인 관려이 있는 중요한 처리는 호스트에 위임해야 한다.
  - 클라이언트는 호스트의 결과를 받고 시각적인 처리만 수행한다.

#### RPC
- 어떤 메서드나 처리를 네트워크를 넘어 다른 클라이언트에서 실행하는 것
- 대부분의 멀티플레이어 API에는 RPC가 구현되어 있다.

### 포톤 준비하기
- 유니티에서 가장 대중적으로 쓰는 플랫폼은 포톤과 미러..?
- 유니티나 언리얼에서 내부적인 솔루션은 이미 배포가 된 상태.
  - 모든 게임에 적합한 구조는 아니다
- 적합하지 않은 경우 별도의 구조를 쓴다
- 로그인, 캐시샵같은 아웃

#### [포톤 AIP 공식문서](https://doc-api.photonengine.com/en/PUN/current/)



### PhotonView
- 네트워크를 통해 동기화될 모든 게임 오브젝트는 해당 컴포넌트를 가진다
- 다른 플레이어들이 이동했을 때의 동기화도 담당
- 해당 컴포넌트에 포함된 정보는 다른 클라이언트에게도 동기화되어야 하는 데이터
- PhotonView가 관측할 컴포넌트는 Observed Components 리스트에 할당
  - IPunObservable 인터페이스를 상속한 컴포넌트만 관측할 수 있다.
- 동기화 방법(Synchronization)
  - Off : 동기화하지 않는다
  - Reliable Delta compressed : 상배장이 최근에 수신한 값과 동일한 값은 송신하지 않는다.
  - Unreliable : 패킷의 수신 여부를 검사하지 않고 지속적으로 송신
  - Unreliable On Change : Unreliable과 동일하나 값의 변화가 감지될 때문 송신
- 동기화 방식은 송수신 데이터의 양과 직결되므로 중요한 내용.

### Photon Transform View 컴포넌트
- 자신의 게임 오브젝트의 트랜스폼 컴포넌트 값의 변화 측정
  - PhotonView 컴포넌트를 사용해 동기화
- PhotonView 컴포넌트 없이 동작할 수 없다.

### Photon Animator View 컴포넌트
- 애니메이터 컴포넌트의 파라미터 동기화, 서로 같은 애니메이션을 재생하도록 한다
- Synchronize Parameters
  - 애니메이션 동기화 품질과 연관.